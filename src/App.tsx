/**
 * 应用根组件（脚手架占位）。
 *
 * 完整的应用界面（工具栏、地图、面板与状态栏）将在功能开发阶段落地，
 * 当前仅提供最小可运行骨架用于验证构建链路。
 */

export function App() {
  return (
    <div className="app">
      <header className="toolbar">
        <span className="toolbar-brand">map.army</span>
      </header>
      <main className="app-main">
        <p>军事标图工作区开发中……</p>
      </main>
    </div>
  );
}
