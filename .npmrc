# 项目级 npm 配置
# 说明：系统盘（C 盘）剩余空间不足，将 npm 缓存目录重定向到项目内，
# 避免安装依赖时出现 ENOSPC 错误。该目录已被 .gitignore 忽略。
cache=.npm-cache

# 使用国内镜像源加速依赖安装
registry=https://registry.npmmirror.com/

# 依赖树上浮安装，减少重复包体积
install-strategy=hoisted

# 安装时严格校验（避免静默使用损坏的包）
audit=false
fund=false
