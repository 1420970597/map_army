# 后端迁移审查记录

基点：`07aa1026d0d8a8431672bc68b9d103b760de5483`；代码范围至 `9c598ba354324979bcc21f8c944be813d0362f58`；最终修复提交另见后续 Git 记录。依据为 `AGENTS.md`、`docs/PROJECT_MEMORY.md`、`docs/TODO-python-mysql-s3-backend-2026-09.md` 和原站一手文档。

尝试按 code-review 技能启动独立双代理，工具返回代理线程数量已达上限。本记录是本地审查与实际回归证据，不冒称独立人工或双代理批准。

## 规范轴

- 未发现遗留的文档规范硬性违规：中文代码注释、Conventional Commits、独立分支/工作树、版本化迁移、真实 MySQL/S3 测试、Compose 交付及保留旧卷均有对应实现。
- 可能的 Divergent Change（判断项）：同步协调模块同时连接文档、资料和目录，工作空间组件包含多个管理页签。当前对外接口集中、持久化规则已移入独立 Python 模块，并有并发/离线/手势验证；本轮未引入仅为拆文件而增加的接口层。
- 旧 Node 代码保留为兼容回归来源，生产 Compose 与 `npm run server` 已指向 Python。格式算法运行在隔离的 Node 子进程中，ADR/README/API 文档明确这一实现选择。

## 需求轴

已修复并验证的具体问题：

1. 断网重连和 409 后刷新必须保留待同步快照、基础版本及资料，不能覆盖本地编辑；手势完成前不提交预览。
2. 首次浏览器迁移保持恢复选择，显式换工作空间不重新导入前一空间的缓存；资料字段不能覆盖 store 操作函数。
3. 老分享迁移可以追加验收期间产生的新版本；复制固定分享按所选版本保留资产；加载新分享版本同时更新模型目录。
4. 历史、项目、分享和模型版本的文件引用保持访问权限与删除保护；图片标准化后的相同保存不制造新修订。
5. PWA API 请求绕过静态缓存；旧 Worker 升级策略后才开始后端同步。浏览器分别验证身份新鲜度、私有文件权限和旧 Worker 升级。
6. 备份逐表/逐对象恢复验证；遇到 S3 相同键但不同内容时拒绝覆盖，相同对象允许重试复用。
7. 模型先校验再入可用目录；不支持的压缩解码格式明确报错，转换失败可查询与重试；真实容器 Blender 转换和重启读取通过。

P02–P11 的代码/接口/浏览器要求已有对应实现与验证；部署入口切换、PR/远端 CI/主线合并按 ToDo 继续记录。AMC 全量几何转换、完整 MSS 覆盖、真实 DEM 不计入本次已完成后端能力。

本地规范轴：0 个遗留硬性违规，1 个非阻断判断项；需求轴：上述问题已修复，未发现代码范围内遗留阻断项。远端 CI 与合并状态不由本记录替代。


后续针对性审查发现并修复：锁定图层的管理字段与页面语义一致；未同步项目/偏好草稿按标签页隔离且支持工作空间恢复；导出、模型目录和另存副本使用当前已打开的分享版本。对应测试已通过。
