# 浏览器 Mover Creator 三维建模 ToDo

2026-09-14 用户要求：仔细阅读 `/root/afsim/afsim_api_zip.zip`，补齐三维模型剩余功能，在浏览器创建和修改自定义模型。以 `496a07f` 主线为基点，在 `feature-mover-creator` 独立工作树执行。用户已明确模型来源不作为功能筛选条件。

本轮实现 Mover Creator 的三维几何工作流：完整内置模板、组件编辑、预览、挂点和模型发布；保留 AMC 其他参数和依赖。气动求解、自动驾驶优化、飞行性能试验属于 Mover Creator 的其他模块，不用三维外观冒充这些计算结果。

- [ ] M01 文档：ZIP 索引和版本核对；结合本机用户手册、AMC 和几何源码，记录单位、轴向、对称、形状和固定组件规则。
- [ ] M02 资产：完整注册 34 个 Vehicle AMC、45 个 Engine 和 12 个 Airfoil；生成所有可见几何组件，逐模型覆盖报告，直接在页面选择。
- [ ] M03 几何：采用原生几何生成算法，支持机身/舱体、机翼/翼面、进气道、座舱、发动机、雷达罩、减速板及参考点；显式英尺转米与坐标变换，保留组件身份。
- [ ] M04 浏览器创作：从模板派生、AMC 导入、组件树、字段编辑、增删/复制组件、对称规则、选中高亮、线框、旋转缩放、撤销重做和三维即时预览。
- [ ] M05 持久化：设计及不可变版本入 MySQL，AMC/依赖/GLB 入 S3；并发检查、工作空间隔离、历史与失败恢复；原始 AMC 字段往返无静默丢失。
- [ ] M06 挂载：页面编辑挂点和安装锚点位置/方向；自定义 GLB 同样支持；兼容部件关联、模型发布版本、详情页拖放装配、只读/锁定/刷新/分享完整回归。
- [ ] M07 交换：AMC 与依赖包、生成 GLB 下载，已发布模型进入统一目录；已使用旧版本不因新版本变化而被覆盖。
- [ ] M08 验证交付：真实后端测试、34 模型结构与视觉检查、浏览器完整流程、CI、Compose、Git 分阶段提交、PR 评审/CI/合并、原站复核及后续差异。

不得将旧 OSGB 目录数量当作 AMC 模板覆盖率，不从互联网搜集替代模型。验收后逐项附证据再勾选。
